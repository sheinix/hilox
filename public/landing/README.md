# Landing assets

Add a single demo GIF that shows the flow:

- **`demo.gif`** — Short screen recording: paste URL → settings → generate → copy thread. Use 16:9 aspect ratio for best display; rounded frame and shadow are applied in the UI.
